After replacing the data in main.py with your own, run obfuscate_fixed.bat.

If you require higher-quality obfuscation, edit the `encrypt.py` file and run it from the console.

If you don't understand the code, use a neural network(AI), and it will help you.
